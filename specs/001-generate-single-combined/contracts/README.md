# Contracts (Documentation-Only)

The authoritative sources do not specify API endpoints or interface schemas.
This directory contains a minimal OpenAPI file that lists endpoints mapped to
UC-01..UC-21 without defining request/response schemas.

## Operations Covered

The spec covers UC-01 through UC-21. When implementation details are defined,
add concrete request/response schemas for the endpoints listed in `openapi.yaml`.
